# Hippie Word Completion

Sublime Text 2/3 style auto completion for ST4: cycle through words, do not show popup. Simply hit `Tab` to insert completion, hit `Tab` again if you don't like it. `Shift+Tab` to cycle backwards.

Installation

Clone this repository into your Packages directory. (Open the command palette and run `Preferences:Browse Packages` to find Packages directory)
